import React from 'react';
import Button from '../UI/Button';

const TopBar = () => {
  return (
    <div className="top-bar">
      <Button text="Sign Up" />
      <Button text="Log In" />
    </div>
  )
}

export default TopBar;